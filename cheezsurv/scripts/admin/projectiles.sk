command /fireball:
  trigger:
    if player is not op:
      send "§cyou are not an admin"
      stop
    
    shoot fireball from player

command /arrow <number=1>:
  trigger:
    if player is not op:
      send "§cyou are not an admin"
      stop
    
    shoot arrow from player at speed arg-1