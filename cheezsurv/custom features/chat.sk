on chat:
    set the chat format to "%player's prefix%%player%§7: §f%message%" if {%player%.nick} is not set
    set the chat format to "%player's prefix%%player% (%{%player%.nick}%)§7: §f%message%" if {%player%.nick} is set